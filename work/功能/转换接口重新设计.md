    由于系统本生是对外集成的，所以为了能适用不同的数据源，需要设计转换层 （转接器）
    在 Web-Defect-Detection-System/app/server 中增加 转接器 目录，设计抽象层结构
    
    在 Web-Defect-Detection-System/app/server/db/models/extra 中 增加 Steel 表， 与 Defects 表
        表中 除了     
            id = Column(Integer, primary_key=True, autoincrement=True)
            line_key = Column(String(64), nullable=False, index=True, comment="产线 key")
            seq_no = Column(Integer, nullable=False, index=True, comment="钢板流水号 SeqNo")
            surface = Column(String(16), nullable=False, comment="表面：top/bottom")
            view = Column(String(32), nullable=False, comment="视角模式：2D/small 等")
    
        一些常规信息(Steel)... steel_no steel_type , steel_len...
            (Defects) name , class_id, image_index, （位于原图上的像素位置），（位于完整图像上的像素位置），（位于steel 上的位置 )....
    
        还记录 old_id (数据原中的 对应数据的 id)
        data 原始数据的json 结构   
    现在的 Web-Defect-Detection-System/app/server/db/models/source/ncdplate.py ncdplatedefect.py 放到 /source/bkv4/
    转换器 增加 /bkv4 模式，现在正在使用的就是 这个模式
    要求： source 下 的数据库 只暴露给 对于的 转换器，API 启动需要指定 转换器，默认为 bkv4(表面检测 4.0 系统),
    转换器独立运行，主要工作为：
        1：数据库同步：将数据源的数据同步到 拓展数据库中 主要是 钢板记录，缺陷相关的表
        2：图像同步：在bkv4 下，图像不需要额外同步，因为bkv4的图像方法，满足当前的图像阅读方法。
        其它缺陷 记录 查询接口，使用