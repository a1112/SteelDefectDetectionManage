    在 app/server/db/models 中 增加 defect_detection 目录，将 defectdetectiondatabbase 数据相关的表
    迁移过去，
    defect_detection 在 defect_detection 中 增加 缺陷标注表，用于记录 缺陷的标注
    表的内容大致包含：
                id = Column(Integer, primary_key=True, autoincrement=True)
                line_key = Column(String(64), nullable=False, index=True, comment="产线 key（对应 DEFECT_LINE_KEY 或映射表配置）")
                seq_no = Column(Integer, nullable=False, index=True, comment="钢板流水号 SeqNo")
                surface = Column(String(16), nullable=False, comment="表面：top/bottom")
                view = Column(String(32), nullable=False, comment="视角模式：2D/small 等")
                
                然后是 用户，默认 为空，除非登陆
                标注方式 （人工，自动）
                缺陷的 像素坐标信息
                类别 id
                类别名称
                缺陷标记
                导出记录 json
                创建时间
                最后修改时间

    当 缺陷标注模式下 增加 已提交 未提交 多选   增加 默认  的 默认切换标注设置（切换默认的缺陷，无 的话 每次标注都会弹窗选择类别 n*n 类别卡片）
    增加 全部提交，清除未提交 按钮，提交后写入数据库
    增加 自动提交 多选