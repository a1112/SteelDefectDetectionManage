在 缺陷分析 下的 shift 快捷键： 在 ；表模式 图标右侧增加一个新的图标，绑定到 shift快捷键，快捷键的功能：
    打开功能时，瓦片加载器作为背景被显示出来，当然：优先加载缺陷小图，瓦片加载器屏蔽上下左右预热，而是根据缺陷命中来做同级别预热
        例如下个缺陷在第 n 张图 的x,y 位置，则优先预热该缺陷对应位置的瓦片，
        复用现在现代化模式下 的瓦片加载器，在请求参数中增加预热规则，同步修改前后端
    我称之为 地图模式  中间大区域 显示的单个缺陷也是 整个 钢板图像的一部分，其实与 现代化模式的 缺陷 界面 的 缺陷查看很类似，为了速度，才增加的缺陷小图的加载模式
  （也考虑到带宽），中间大区域在显示 单个缺陷的时候，实际上带宽够的话，直接显示出瓦片加载器是最好的，但是不一定带宽够，所以先显示缺陷图像，根据缺陷图像位于瓦片
  加载器上的位置来定位瓦片的显示位置与缩放大小，看起来没有显示这个缺陷图像一样（超出区域的缺陷也偏移显示）。
    另外一个窗口下，你的回答：理解了，你说的“地图模式”是这样的逻辑：

  - 中间大区域本质上应该是“整板瓦片视图”，单个缺陷只是在这个大图里的一个局部。
  - 为了带宽和速度：
    1）先用缺陷小图（裁剪图）秒开；
    2）后台启动瓦片加载器，把整板瓦片加载到同一个区域；
    3）瓦片视图的中心点和缩放根据缺陷的 x/y（在整板上的位置）来设置，让最终看到的画面和一开始的缺陷小图几乎一致，只是换成了高精度可平移/缩放的大图；
    4）当缺陷在当前视口之外时，通过偏移和缩放，让它偏移后也能在“地图”上对得上位置。

  同时，“地图模式”本身是针对中间大区域的视图模式（和列表模式按钮、Shift 快捷键绑定），而不是右侧缺陷列表的功能；瓦片预热也要从“同层级的相邻瓦片”改成“按下一个缺陷
  在第 N 帧 x,y 的位置来定向预热”。

  后续我会按这个思路来设计实现：

  - 把 Shift / “地图模式”开关绑定到中间大区域（列表模式按钮附近），而不是缺陷列表。
  - 在地图模式下：
      - 先显示当前缺陷小图；
      - 并行启动瓦片视图，等瓦片加载好后无缝替换/覆盖；
      - 对齐视口中心和缩放，使缺陷在瓦片视图中的位置与小图一致。
  - 调整 tile 请求和预热策略，让“命中缺陷位置”的瓦片优先加载，而不是默认的上下左右预热。

  如果你确认这个交互/行为设计没问题，我下一步就直接在传统模式里接入现有的瓦片查看器（复用现代化模式的组件），并在后端 /images/tile 增加一个“按缺陷位置预热”的可选
  参数来驱动预取逻辑。