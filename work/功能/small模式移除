    现在的API 使用了 2D, small 模式，然而，这时不必要的，因为 使用最大尺寸加载图像增加 缩放参数就可以实现低分辨率的方法进行图像返回，而且多API 服务会
    更加难以管理，更改如下：
        1：保留 views 参数与API构建逻辑 （功能保留）
        2，移除views参数中 small 的配置，
        3.前端 的 数据源与性能配置 界面中，增加 下拉的 数据缩放 设置，25%，50%，75%，100%
        4，对于缺陷，瓦片 等一切图像请求 都增加缩放参数，缓存逻辑不变，但是图像返回在设置的基础上缩放，瓦片大小，也会随之更改
        5，注意缺陷的绘制位置