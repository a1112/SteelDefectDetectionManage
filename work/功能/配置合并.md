 我想 移除 Web-Defect-Detection-System/configs 中 server_small.json 文件
 因为 内容几与 server.json 一致，仅 在 map.py 中 增加 
    "views":{
            "2D":{
                    "frame_width": 16384,
                    "frame_height": 1024,
                     "pixel_scale": 1.0, 
                    },
            "small":{
                        "frame_width": 8192,
                        "frame_height": 512,
                        "pixel_scale": 0.5,
                    }
            } 的 特点配置 
在 configs 中 创建 template/ 目录 将基础的  server.json , map.json, DefectClass.json 存储，net_tabel/DATA 中 的生成依据。
 
移除多于的 server.json 

不再 依赖 Web-Defect-Detection-System/configs/server.json Web-Defect-Detection-System/configs/server_small.json 而是优先依赖对应
    net_tabel/DATA/ 下，对应设置的的  map.json server.json  移除现在的 Web-Defect-Detection-System/configs/net_tabel/DEFAULT，
    因为改到了 template/
 移除现在 main.json,改为增加 version.json，用于管理服务端版本。

ennnn 干脆移除 Web-Defect-Detection-System/configs/net_tabel，不再管理不同现场的配置数据，新的方法如下：
服务第一次启动时，生成：configs/current 目录
            拷贝 server.json , map.json, DefectClass.json 到该目录。
            服务根据 map.json 构建 服务，优先 在 configs/current/{line_name} 寻找 DefectClass.json 否则使用 configs/current/DefectClass.json
update_nginx.cmd，根据 configs/current/map.json 在 configs/current/ 生成 nginx.conf 并且拷贝到 plugins/platforms/windows/nginx/conf 启动

服务构建：
    根据 map.py中的 "views“ lines 来构建 api服务，，依旧是一级级覆盖，生成 len(lines)*len(views) 的服务数量。

前端的一些服务设置更改也是改到 configs/current