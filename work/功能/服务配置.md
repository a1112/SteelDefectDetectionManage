将server.json 中
    "max_cached_frames": 64,
    "max_cached_tiles": 256,
    "max_cached_mosaics": 8,
    "max_cached_defect_crops": 256,
    "cache_ttl_seconds": 120,
    "defect_cache_enabled": true,
    "defect_cache_expand": 100,
    "disk_cache_enabled": true,
    "disk_cache_max_tiles": 2000000,
    "disk_cache_max_defects": 10000000,
    "disk_cache_scan_interval_seconds": 5,
    "disk_cache_cleanup_interval_seconds": 60,
    "disk_cache_precache_enabled": true,
    "disk_cache_precache_levels": 4
    修改为 "cached":{
        max_frames:64    如果是 -1 表示全部
        ...
    } 与 "images" 相同级别

移除 map.json 中 的 defaults

添加产线是 会在 generated 根据key 添加目录 （不再是 name），修改 key 时 会 修改 generated 中文件夹名称

现在 server.json 就只有 database, images, cache 的配置了。
继承 与 模板修改
模板的修改会影响到 configs/current/server.json 但是 template 是无法暴露给前端的。

产线添加时 configs/current/generated/{key}/{view}/server.json  中 的 database, images, cache都处于 继承 状态 ，images：{} 为空
    输入框为类似的提示输入，表示继承状态，，编辑后 configs/current/generated/{key}/{view}/server.json 中增加对应的设置 ，清空则移除，继续使用继承值
DefectClass.json 也符合模板逻辑 
继续修改 服务设置 界面，使得模板编辑 database, images, cache DefectClass。  产线编辑 （配置与运行）。产线独立参数编辑 （继承或者 独立的 database, images, cache DefectClass（这个不符合局部继承） ）
