Web-Defect-Detection-System 启动后 在 构建 map.json 指定的 lines 外，构建 独立的 配置中心服务
配置中心服务 启动 0.0.0.0  8119
配置中心服务功能：
    1.配置中心 以 /config 路由 开头、
        /config/api_list  获取启动的服务信息，
        /config/restart/{line}  重启对应服务名称的服务 /config/restart 重启全部服务
    2，路由生成：
        启动时根据实际情况生成并应用路由表：
            /config ->  本机：8119   将现在 前端的 /api_list 改为 /config/api_list
            服务端，前端 api 的 /health 改到 /api/health
            /api/{产线} 根据 map.json 中的 lines 转发到 对应的端口
            /small--api/{产线} 改到 lines 中 对应端口+100 {/small--api-> /api}
        前端 {产线}/api 改到 /api/{产线} 的模式

根据当前操作系统生成 对应的 nginx 脚本并且应用脚本