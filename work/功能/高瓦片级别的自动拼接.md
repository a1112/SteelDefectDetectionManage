    前情提要·：
    目前的最大瓦片级别，是根据瓦片宽高，图像宽度自动计算出来的，L0 是原始大小,然后每增长一级，缩放就是原来的1/2  现在 16384 长度 的图像，最大瓦片级别应该是L4(L0-L4)
        1:在现在的设置界面中的 系统信息 的 API 信息中， 增加 瓦片大小，最大瓦片级别的显示 （注意接口增加对应参数）
    然而，并不是所有的界面都能够满足需求，也不是一定要 1024 大小的图像。
        2：GET /api/images/tile? api 增加 width,height  参数，用来 resize 后返回瓦片图像，没有  resize 的情况：根据实际大小进行返回
        二次确认：现在瓦片，预热都是在 需要 的情况下，才对图像数据转换为PIL,opencv 等 对象，负责都是用二进制即可满足 （在不重新resize,有瓦片cache 的情况）
        瓦片也不会自动填纯，而是智能大小，例如高级别瓦片可能出现高度上的图像不够，那就可能出现 1024*512... 的图像
    需要更改级别的情况：
        瓦片请求不再强行限制在L4,分布图根据自身的显示区域大小计算级别，继续以 1024 16384  为例，L5 的图像宽度将 是 512,高度默认填充到 1024 或没有图像了
        以此类推
        3：高级别图像的请求：L5->如果有瓦片缓存就进行读取返回->没有：读取低级别瓦片拼接，resize,顺便保存 中间层瓦片。
    复述需求，我来确认    
    
    level=5 要先走判断，因为已只 L4宽度覆盖了整个图像，那会就不会横向找，就会取 1*2 的L4,同样的，L6取 1*4 的，但是因为原始图像数量已知（假设16k*1024 在1024 的瓦片下 只有16张 图，那么 L4 就只有1张瓦片了（1024*1024），L5应该返回 512*1024的 在这种情况就是 512*512，L6 就是 256*256） 如果是24张 L4 的第二张瓦片就  
      是1024*512 L5就是 512 * （1024+512）/2 同理
