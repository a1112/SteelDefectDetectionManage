API 位置： ./Web-Defect-Detection-System
前端位置： ./Figmaaidefectdetectionsystem
现在，将进行大量的接口调试，你需要同时修改前端，后端部分代码，接下来，我会陆续提出需求，将其实现，明白请回复收到
关于路由，参考 plugins/platforms/windows/nginx/conf/nginx.conf
现在，我需要对前端的界面进行API调试 （特别是新增加的 传统仪表盘界面）

1：测试修复：
在前端，我看到了  /api/config/speed_test?chunk_kb=1024&total_mb=1024 请求，这个请求是行不通的，修改如下：
    前端测试（数据源与性能配置界面的测速按钮） 调用的接口应该为 /api/{line_key}/speed_test（/small-api/{line_key}/speed_test）,对于服务器的测速为
    /config/speed_test 在 下载带宽测试 界面中 增驾对测试产线名称的显示，在开始测试按钮同排的最左侧，增加 测试配置中心 的多选框，点击后会调用
    /config/speed_test 的测试

2：IO优化：

缺陷分析图像加载，


3：缓存功能增加对 缺陷图像的缓存
    目前存在 cache\2D\tile or cache\small\tile 的 瓦片缓存机制
    增加 cache\{模式}\defect 文件夹 对缺陷切片进行缓存。
    配置增加：
            在 server.json / server_small.json 增加 缺陷缓存  缺陷缓存最大裁剪保留（裁剪缺陷时的拓展像素，默认 100）
            在 cache.json 记录 缺陷缓存  缺陷缓存最大裁剪保留 等参数
    缓存刷新机制：
        缓存 线程/进程 通过 读取 cache.json 以及比对 服务启动的配置界面进行重新缓存或者补充缓存（例如 "max_level": 4 改到 "max_level": 5）
    回退机制：
        当缺陷缓存命中失败将会回退到图像上重新切割，此时，如果设置了缓存，会通知cache优先对改板进行缓存。
    cache数据表增加：
        在DefectDetectionDatabBase 数据库中，增加 缓存记录表，基本是 cache.json中的记录，优先作为缓存依据，通过产线key ，流水号 作为 依据
        增加 缺陷统计表，也根据 产线key ，流水号 来缓存不同缺陷类别的统计 产线key ，流水号 缺陷名称 数量  
    增加钢板判级表，缺陷判级表，钢板标记标，缺陷标记表
        